export interface FlightModel {
  origin: string;
  destination: string;
}

export interface FlightPlaces {
  airportCode: string;
  cityName: string;
  cityCode: string;
  countryName: string;
  countryCode: string;
  airportName: string;
  nationality: string;
  currency: string;
  airport: number;
}
