import { fonts } from './fonts';
import { margins, space, radii, radius } from './margins';

export const theme = {
  fontFamilies: fonts,
  margins,
  radii,
  radius,
  space,
};

export default theme;
